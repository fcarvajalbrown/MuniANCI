{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacidad minima de la ventana principal: solo el nucleo (invoke, eventos). El frontend no usa las APIs JS de dialogo/shell — el guardado con dialogo nativo y el revelado de carpeta ocurren en Rust (export_report.rs), que no pasa por la ACL del webview. Retirar dialog/shell del webview reduce su superficie (least-privilege).",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
