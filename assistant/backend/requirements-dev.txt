# MuniGPT backend development/test dependencies.
#
#   pip install -r requirements-dev.txt
#
# Kept separate from requirements.txt so the shipped installer does not bundle
# the test toolchain.
-r requirements.txt

pytest
pip-audit             # dependency vulnerability gate (mirrors CI python-audit job)
cyclonedx-bom         # Python SBOM generation (mirrors CI sbom job)
