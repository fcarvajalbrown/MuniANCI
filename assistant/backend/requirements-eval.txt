# Offline eval-judge layer (ROADMAP 0.4.0) — heavy, isolated from the runtime deps.
# Only needed to run eval/eval_judge.py (the LLM-judged faithfulness layer). The
# deterministic gate (eval/eval_harness.py) needs none of this. The judge is the LOCAL
# llama.cpp server (OpenAI-compatible), so nothing leaves the machine.
#
#   pip install -r requirements.txt -r requirements-eval.txt
#
# PIN NOTE: ragas 0.4.3 imports internal langchain_community paths that only exist in
# the langchain 0.3.x family. Installing unpinned pulls langchain 1.x and breaks the
# ragas import (ModuleNotFoundError: langchain_community.chat_models.vertexai). Keep the
# langchain family on 0.3.x.
ragas==0.4.3
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-openai>=0.3,<0.4

# DeepEval is optional and heavier (can pull large ML deps); enable if you want its
# metrics in addition to Ragas. Left commented to keep the eval install lean.
# deepeval
