# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Corpus, DB, model weights, and llama.cpp binaries — too large for git, ship via installer
backend/corpus/
backend/db/
backend/models/
backend/bin/

# Per-comuna downloaded corpus + built swappable DBs (data, not code). The
# fetch scripts under corpus_muni/ stay tracked for provenance.
backend/corpus_muni/**/*.pdf
backend/corpus_muni/**/*.txt
backend/db_*/

# Normativa del sector Defensa: los PDF viven en docs/ del repositorio y se copian
# aquí solo para ingestar. El README de la carpeta queda versionado como procedencia.
backend/corpus_defensa/*.pdf

# Python venv
.venv/

# Environment
.env
config.json

# Node build artifacts (the chat UI now lives in gui/frontend; the former
# standalone frontend/ and Electron shell were removed post-merge)
node_modules/
dist/
.vite/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log

# Stray build artifacts
*.exe